Just add it to your web root and change config.php file...
==========================================================

This is very simple Jabberd2 web interface.

Currently supports:
	- User Management
	- ...

	P.S. It works directly with DB (Mysql only!)
